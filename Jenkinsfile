pipeline {
    agent any
    stages {
        stage('Run Python') {
            steps {
                echo 'Running Python program...'
                sh 'python3 hello.py'
            }
        }
    }
}
